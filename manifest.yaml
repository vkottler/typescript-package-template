---
default_target: groups-all

includes:
  - config/includes/license.yaml
  - config/includes/funding.yaml

compiles:
  - name: local
    schemas:
      - config/schemas
    configs:
      - local/configs

groups:
  - name: all
    dependencies:
      - groups-license
      - groups-funding
